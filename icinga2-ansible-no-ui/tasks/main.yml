---
# tasks file for icinga2-ansible-no-ui

- name: Add the OS specific varibles
  include_vars: "icinga2_{{ ansible_os_family }}_install.yml"

- include: icinga2_configure.yml
  tags: 
   - icinga2_configure
