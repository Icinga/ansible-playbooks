# {{ ansible_managed }}

{% for key,value in check_commands.items() | list %}
object CheckCommand "{{ key }}" {
  import "plugin-check-command"

  command = [
    PluginDir + "/{{ key }}",
    {{ value }}
  ]
}

{% endfor %}
